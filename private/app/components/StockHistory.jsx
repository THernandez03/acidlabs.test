import React from 'react';

export default class StockHistory extends React.Component {
  static propTypes = {
  };
  static defaultProps = {
  };

  render(){
    return (
      <div></div>
    );
  }
}
